export default interface Product {
    id: string;
    title: string;
    description: string;
    coverImageUrl: string;
    boardImageUrl: string;
    basePrice: number;
    additionsPrice: number;
}